The input.txt is sample input paragraph.

First running the file setting.py to get the training dataset which is stored in train.txt.

Then running the multi-sense.py with the commond line:
	python multi-sense.py train.txt

The output stored in the out.txt.